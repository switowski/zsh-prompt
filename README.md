# zsh-prompt

Custom prompt for zsh (based on [steeef](https://github.com/danihodovic/steeef) prompt).
Requires prezto.

### Install with zgen

Add `zgen load switowski/zsh-prompt` to your `.zshrc` (before the `zgen save` and after the `zgen prezto`).
